# 安全策略

## 报告漏洞

请不要在公开 Issue 中披露未修复的安全漏洞。优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口，并提供影响范围、复现步骤和建议修复方式。

## 凭据与部署

- 不要提交 `.env`、`.local.env`、数据库密码、模型 API Key 或 `BI_DATASOURCE_MASTER_KEY`。
- 首次部署后立即修改初始化脚本中的默认管理员密码。
- 生产环境必须通过环境变量或外部密钥管理系统注入凭据，并限制元数据库与业务数据源的网络访问。
- 洞察历史包含报表数据快照，应按业务数据的同等等级执行访问控制、备份和保留策略。

项目维护者会确认有效报告，并在修复可用后协调披露时间。
